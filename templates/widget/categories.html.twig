<div class="p-4">
    <h4 class="fst-italic">Catégories</h4>
    <ol class="list-unstyled mb-0">
{#         {%  for category in categories %}
            <li><a class="text-decoration-none" href="{{ path('category_show', { 'slug': category.slug }) }}">{{ category.name }}</a></li>
        {% endfor %} #}
        <li>
            <a class="text-decoration-none" href="#">tutoriel C++</a>
        </li>
        <li>
            <a class="text-decoration-none" href="#">tutoriel PHP/MySQL</a>
        </li>
        <li>
            <a class="text-decoration-none" href="#">tutoriel Symfony</a>
        </li>
    </ol>
</div>